# Preview 3.9 Settings compatibility audit

Source audit of every leaf preference in the existing main/advanced Settings XML, plus dynamic controls below. **Active** means the real handler remains available subject to its pre-existing device/account/capability gate; it does not claim every option was exercised on hardware. No preference key, default or saved value was deleted/reset by this presentation pass. Existing platform-dependent hiding/validation remains. Opening Preview Settings no longer clears saved decoder/audio-interface selections or the saved software-decoding choice; their existing playback handlers remain unchanged.

| Preference | Underlying behaviour found | Active/Disabled | Reason |
|---|---|---|---|
| Build (`preferences_version`) | Displays the installed build and existing release information. | Active | Still used; existing device, account and capability gates retained. |
| User interface language (`ui_lang`) | CustomApplication loads the chosen locale; the existing change handler reloads the interface. | Active | Still used; existing device, account and capability gates retained. |
| Force software decoding (`force_software_decoding`) | PlayerActivity/MediaFactory select the requested decoder/backend. | Active | Still used; existing device, account and capability gates retained. |
| Choose decoder (`dec_choice`) | PlayerActivity/MediaFactory select the requested decoder/backend. | Active | Still used; existing device, account and capability gates retained. |
| Choose audio interface (`audio_interface_choice`) | MediaFactory chooses the audio output interface/decoder. | Active | Still used; existing device, account and capability gates retained. |
| Choose audio decoder (`audio_decoder_choice`) | MediaFactory chooses the audio output interface/decoder. | Active | Still used; existing device, account and capability gates retained. |
| Fullscreen video with display cutouts (`enable_cutout_mode_short_edges`) | PlayerActivity, SurfaceController and SubtitleManager apply display-cutout sizing. | Active | Still used; existing device, account and capability gates retained. |
| Cut display equally on both sides (`enable_cutout_both_sidesx`) | PlayerActivity, SurfaceController and SubtitleManager apply display-cutout sizing. | Active | Still used; existing device, account and capability gates retained. |
| Audio passthrough (`force_audio_passthrough_multiple`) | Audio capability and player setup configure passthrough; existing HDMI/platform gates remain. | Active | Still used; existing device, account and capability gates retained. |
| Force audio passthrough (`force_passthrough`) | Audio capability and player setup configure passthrough; existing HDMI/platform gates remain. | Active | Still used; existing device, account and capability gates retained. |
| Enable playback speed (`playback_speed`) | VideoPreferencesCommon and PlayerService enable speed control; the AVOS tempo/AudioTrack path follows the saved choice. | Active | Still used; existing device, account and capability gates retained. |
| Use AudioTrack for playback speed (`audio_speed_audiotrack`) | VideoPreferencesCommon and PlayerService enable speed control; the AVOS tempo/AudioTrack path follows the saved choice. | Active | Still used; existing device, account and capability gates retained. |
| Dynamic audio/video delay estimation (`enable_dynamic_audio_delay`) | LibAvos dynamic audio/video delay estimation; existing passthrough gate remains. | Active | Still used; existing device, account and capability gates retained. |
| Platform spatialization (`player_spatialization_enabled`) | PlayerActivity configures the platform spatialiser where supported. | Active | Still used; existing device, account and capability gates retained. |
| Disable audio stereo downmix (`disable_downmix`) | PlayerActivity selects stereo downmix independently for phone/tablet and Android TV. | Active | Still used; existing device, account and capability gates retained. |
| Parser synchronisation mode (`parser_sync_mode`) | LibAvos parser synchronisation mode. | Active | Still used; existing device, account and capability gates retained. |
| Force audio stereo downmix (`enable_downmix_androidtv`) | PlayerActivity selects stereo downmix independently for phone/tablet and Android TV. | Active | Still used; existing device, account and capability gates retained. |
| Enable automatic refresh rate switch (`enable_tv_refreshrate_switch_mode`) | Player switches the display refresh rate during preparation. | Active | Still used; existing device, account and capability gates retained. |
| Dolby Vision mode (`dolby_vision_mode`) | Player chooses the existing Dolby Vision handling mode. | Active | Still used; existing device, account and capability gates retained. |
| Prefer original audio track (`prefer_original_audio_track`) | PlayerService selects preferred/original audio tracks. | Active | Still used; existing device, account and capability gates retained. |
| Audio preferred language (`favAudioLang`) | PlayerService selects preferred/original audio tracks. | Active | Still used; existing device, account and capability gates retained. |
| Enable automatic 3D TV switch (`activate_tv_switch`) | StereoMergeArchosEffect controls the existing automatic 3D display switch. | Active | Still used; existing device, account and capability gates retained. |
| Stream buffer size (`stream_buffer_size`) | PlayerActivity passes buffer/I-frame limits to the native player. | Active | Still used; existing device, account and capability gates retained. |
| Max i-frame size (`stream_max_iframe_size`) | PlayerActivity passes buffer/I-frame limits to the native player. | Active | Still used; existing device, account and capability gates retained. |
| Allow using another video player (`allow_3rd_party_player`) | PlayUtils opens the existing external-player chooser; direct Continue Watching uses the same route. | Active | Still used; existing device, account and capability gates retained. |
| Smart recent categories (`smart_recently_rows`) | LoaderUtils/MainFragment switch legacy recent-row queries; Preview builds its own merged rows. | Disabled in Preview | Not applicable to the approved Preview layout. Dimmed, non-selectable and skipped by D-pad; key, saved value and legacy implementation preserved. |
| Enable projector mode (`player_projector_mode_key`) | SurfaceController applies projector presentation mode. | Active | Still used; existing device, account and capability gates retained. |
| Hide watched videos (`hide_watched`) | LoaderUtils filters supported classic browsers/playlists. The Preview full-library query does not apply this flag. | Active | Retained because other reachable library/playlist routes still use it; not represented as a Preview full-grid filter. |
| Ignore initial articles when sorting (`sort_ignore_articles`) | SortUtils and existing browser/channel loaders choose normalised title ordering. Preview uses its own selected sort. | Active | Retained for existing browsing/channel behaviour; Preview sorting scope is separate. |
| User interface mode (`uimode`) | UiChoiceDialog/EntryActivity select TV or phone interface; existing device visibility retained. | Active | Still used; existing device, account and capability gates retained. |
| User interface mode (`uimode_leanback`) | UiChoiceDialog/EntryActivity select TV or phone interface; existing device visibility retained. | Active | Still used; existing device, account and capability gates retained. |
| Interface size (`ui_zoom`) | DensityTweak changes interface density through the existing activity-result/restart handler. | Active | Still used; existing device, account and capability gates retained. |
| Display resume box (`display_resume_box`) | Phone/tablet BrowserLayout displays its resume box. | Active | Existing TV visibility gate retained; not a Preview Home control. |
| Remember video brightness. (`reset_brightness_on_start`) | MainActivity/BrightnessDialog restore or reset video brightness. | Active | Existing phone/device visibility retained. |
| Hide controls on pause. (`hide_controls_on_pause`) | PlayerController changes pause-time control timeout, including the Preview HUD. | Active | Still used; existing device, account and capability gates retained. |
| Color theme (`app_theme`) | ThemeManager colours existing native screens/dialogs; the approved Preview surfaces use a locked Slate palette. | Active | Still affects supported native routes; does not override locked Preview colours. |
| Always start in TV user inferface (`always_leanback_on_tv_key`) | UiChoiceDialog/EntryActivity select TV or phone interface; existing device visibility retained. | Active | Still used; existing device, account and capability gates retained. |
| Separate animations from movies and TV shows (`separate_anime_movie_show`) | Legacy collection/movie/TV loaders separate animation; Preview has the locked combined full libraries. | Disabled in Preview | Not applicable to the approved Preview layout. Dimmed, non-selectable and skipped by D-pad; key, saved value and legacy implementation preserved. |
| Show last added row (`show_last_added_row`) | MainFragment toggles a legacy Home row; the Preview Home branch bypasses that row construction. | Disabled in Preview | Not applicable to the approved Preview layout. Dimmed, non-selectable and skipped by D-pad; key, saved value and legacy implementation preserved. |
| Show last played row (`show_last_played_row`) | MainFragment toggles a legacy Home row; the Preview Home branch bypasses that row construction. | Disabled in Preview | Not applicable to the approved Preview layout. Dimmed, non-selectable and skipped by D-pad; key, saved value and legacy implementation preserved. |
| Show watching/up next row (`show_watching_up_next_row`) | MainFragment toggles a legacy Home row; the Preview Home branch bypasses that row construction. | Disabled in Preview | Not applicable to the approved Preview layout. Dimmed, non-selectable and skipped by D-pad; key, saved value and legacy implementation preserved. |
| Reset last played row (`reset_last_played_row`) | Existing confirmation/DbUtils handler clears database playback history. | Active | Existing destructive-action confirmation retained; no automatic reset added. |
| Show all movie row (`show_all_movies_row`) | MainFragment toggles a legacy Home row; the Preview Home branch bypasses that row construction. | Disabled in Preview | Not applicable to the approved Preview layout. Dimmed, non-selectable and skipped by D-pad; key, saved value and legacy implementation preserved. |
| All movie row sort order (`preferences_movie_sort_order`) | Existing legacy-row and Android TV ChannelManager sorting. Preview libraries have independent Sort controls. | Active | Retained because launcher-channel behaviour still consumes these settings. |
| Try new UI (`try_new_ui`) | Chooses the Preview presentation at startup; classic presentation code remains. | Active | Still used; existing device, account and capability gates retained. |
| Show documentaries (`show_documentaries`) | MainFragment toggles a legacy Home row; the Preview Home branch bypasses that row construction. | Disabled in Preview | Not applicable to the approved Preview layout. Dimmed, non-selectable and skipped by D-pad; key, saved value and legacy implementation preserved. |
| Show all TV show row (`show_all_tv_shows_row`) | MainFragment toggles a legacy Home row; the Preview Home branch bypasses that row construction. | Disabled in Preview | Not applicable to the approved Preview layout. Dimmed, non-selectable and skipped by D-pad; key, saved value and legacy implementation preserved. |
| All TV show row sort order (`preferences_tv_show_sort_order`) | Existing legacy-row and Android TV ChannelManager sorting. Preview libraries have independent Sort controls. | Active | Retained because launcher-channel behaviour still consumes these settings. |
| Show all animations row (`show_all_animes_row`) | MainFragment toggles a legacy Home row; the Preview Home branch bypasses that row construction. | Disabled in Preview | Not applicable to the approved Preview layout. Dimmed, non-selectable and skipped by D-pad; key, saved value and legacy implementation preserved. |
| All animations row sort order (`preferences_animes_sort_order`) | Existing legacy-row and Android TV ChannelManager sorting. Preview libraries have independent Sort controls. | Active | Retained because launcher-channel behaviour still consumes these settings. |
| OpenSubtitles credentials (`subtitles_credentials`) | Existing OpenSubtitles account dialog and credential handling. | Active | Still used; existing device, account and capability gates retained. |
| Hide subtitles by default (`subtitles_hide_default`) | PlayerService selects/hides subtitles according to saved preferences. | Active | Still used; existing device, account and capability gates retained. |
| Subtitles reading language (`favSubLang`) | PlayerService selects/hides subtitles according to saved preferences. | Active | Still used; existing device, account and capability gates retained. |
| Subtitles download languages (`languages_list`) | Existing subtitle download language filtering. | Active | Still used; existing device, account and capability gates retained. |
| Subtitles encoding (`codepage`) | MediaFactory/native subtitle decoding character set. | Active | Still used; existing device, account and capability gates retained. |
| Upgrade to ad-free version (`trakt_getfull`) | Existing ad-free upgrade entry with its build/product visibility gate. | Active | No new purchase or integration behaviour introduced; existing visibility retained. |
| Sign In (`trakt_signin`) | Existing Trakt authorisation and sign-out handlers. | Active | Still used; existing device, account and capability gates retained. |
| Live scrobbler (`trakt_live_scrobbling`) | Existing Trakt service playback scrobbling and resume synchronisation. | Active | Still used; existing device, account and capability gates retained. |
| Sync trakt resume position (`trakt_sync_resume`) | Existing Trakt service playback scrobbling and resume synchronisation. | Active | Still used; existing device, account and capability gates retained. |
| Force re-upload to Trakt (`trakt_force_push`) | Existing explicit Trakt upload/import actions and confirmations. | Active | Still used; existing device, account and capability gates retained. |
| Force re-import from Trakt (`trakt_force_pull`) | Existing explicit Trakt upload/import actions and confirmations. | Active | Still used; existing device, account and capability gates retained. |
| Sign out of Trakt.tv (`trakt_wipe`) | Existing Trakt authorisation and sign-out handlers. | Active | Still used; existing device, account and capability gates retained. |
| Use alternate SMB implementation (SMB2+) (`pref_smbj`) | Existing SMB implementation/version selection in FileCore and connection setup. | Active | Still used; existing device, account and capability gates retained. |
| Enable SMBv2 support (`pref_smbv2`) | Existing SMB implementation/version selection in FileCore and connection setup. | Active | Still used; existing device, account and capability gates retained. |
| SMB resolver order (`pref_smb_resolv`) | Existing SMB resolution/discovery configuration and discovery restart handlers. | Active | Still used; existing device, account and capability gates retained. |
| Disable SMB TCP discovery (`pref_smb_disable_tcp_discovery`) | Existing SMB resolution/discovery configuration and discovery restart handlers. | Active | Still used; existing device, account and capability gates retained. |
| Disable SMB UDP discovery (`pref_smb_disable_udp_discovery`) | Existing SMB resolution/discovery configuration and discovery restart handlers. | Active | Still used; existing device, account and capability gates retained. |
| Disable SMB mDNS discovery (`pref_smb_disable_mdns_discovery`) | Existing SMB resolution/discovery configuration and discovery restart handlers. | Active | Still used; existing device, account and capability gates retained. |
| Use newer SFTP implementation (`pref_sshj`) | Existing SFTP implementation selection. | Active | Still used; existing device, account and capability gates retained. |
| Create remote thumbnails (`pref_create_remote_thumbs`) | Video provider/presenters request thumbnails for supported remote media. | Active | Still used; existing device, account and capability gates retained. |
| Resume points and position bookmarks on network (`network_bookmarks`) | PlayerService/PlayerActivity read and write external resume/bookmark files. | Active | Still used; existing device, account and capability gates retained. |
| Prefer VPN network (`pref_network_prefer_vpn`) | NetworkState chooses eligible network connections/VPN routing. | Active | Still used; existing device, account and capability gates retained. |
| Enable network share on mobile connection (`vpn_mobile`) | NetworkState chooses eligible network connections/VPN routing. | Active | Still used; existing device, account and capability gates retained. |
| Automatically get info for new videos (`enable_auto_scrap_key`) | AutoScrapeService automatically obtains metadata after import; separate from network scan scheduling. | Active | Still used; existing device, account and capability gates retained. |
| Video description language (`favScraperLang`) | Existing scraper description language selection. | Active | Still used; existing device, account and capability gates retained. |
| Process \'.nfo\' files (`network_nfo_parse`) | Network scanning/scraping reads existing NFO metadata. | Active | Still used; existing device, account and capability gates retained. |
| Use already scraped Information (`scrape_from_database_key`) | AutoScrapeService reuses stored matching metadata. | Active | Still used; existing device, account and capability gates retained. |
| Auto-export after search (`nfo_export_auto`) | Existing post-scrape NFO export behaviour. | Active | Still used; existing device, account and capability gates retained. |
| Rescrape all (`rescrap_all_prefkey`) | Existing rescrape confirmations and AutoScrapeService flags. | Active | Still used; existing device, account and capability gates retained. |
| Rescrape all movies (`rescrap_all_movies_prefkey`) | Existing rescrape confirmations and AutoScrapeService flags. | Active | Still used; existing device, account and capability gates retained. |
| Rescrape all collections (`rescrap_all_collections_prefkey`) | Existing rescrape confirmations and AutoScrapeService flags. | Active | Still used; existing device, account and capability gates retained. |
| Recreate sort titles (`recreate_sort_titles_prefkey`) | Existing database sort-title rebuild action. | Active | Still used; existing device, account and capability gates retained. |
| Export already scraped videos (`export_manual_prefkey`) | Existing manual NFO metadata export. | Active | Still used; existing device, account and capability gates retained. |
| Export media database (`db_export_manual_prefkey`) | Existing media database export. | Active | Still used; existing device, account and capability gates retained. |
| Export media library + credentials + shortcuts (`media_library_export_prefkey`) | Existing complete library/credentials/shortcuts backup and restore workflow with confirmations. | Active | Still used; existing device, account and capability gates retained. |
| Import media library + credentials + shortcuts (`media_library_import_prefkey`) | Existing complete library/credentials/shortcuts backup and restore workflow with confirmations. | Active | Still used; existing device, account and capability gates retained. |
| Adult video scraping (`enable_adult_scrap_key`) | Existing scraper adult-content query flag. | Active | Still used; existing device, account and capability gates retained. |
| Rescan storage (`rescan_storage`) | Existing local-storage rescan handler. | Active | Still used; existing device, account and capability gates retained. |
| Display all files in browser (`preference_display_all_files`) | Existing file-browser filtering helper includes non-video files. | Active | Still used; existing device, account and capability gates retained. |
| Download path (`preferences_torrent_path`) | Existing torrent download directory and blocklist configuration. | Active | Still used; existing device, account and capability gates retained. |
| Blocklist (`preferences_torrent_blocklist`) | Existing torrent download directory and blocklist configuration. | Active | Still used; existing device, account and capability gates retained. |
| Enable Nova sponsor link (`enable_sponsor`) | Existing sponsor-link preference under its existing build capability gate. | Active | Still used; existing device, account and capability gates retained. |
| Subtitles provided by (`preferences_video_os`) | Existing service attribution/information handlers. | Active | Still used; existing device, account and capability gates retained. |
| Movie/TV Show information provided by (`preferences_video_tmdb`) | Existing service attribution/information handlers. | Active | Still used; existing device, account and capability gates retained. |
| Video scrobbler provided by (`preferences_video_trakt`) | Existing service attribution/information handlers. | Active | Still used; existing device, account and capability gates retained. |
| Regular preferences (`preferences_video_advanced_quit`) | Returns from advanced to regular preference presentation. | Active | Still used; existing device, account and capability gates retained. |
| Make remaining time negative in player (`make_time_negative`) | PlayerController formats the remaining playback time; remains applicable to the new HUD. | Active | Still used; existing device, account and capability gates retained. |
| Hide trailer row in movies (`hide_trailer_row`) | Legacy Details trailer-row visibility; Preview uses a conditional real Trailer action. | Disabled in Preview | Not applicable to the approved Preview layout. Dimmed, non-selectable and skipped by D-pad; key, saved value and legacy implementation preserved. |
| Show movies/tv shows by rating (`show_by_rating`) | Legacy MainFragment rating browse rows; absent from the approved Preview layout. | Disabled in Preview | Not applicable to the approved Preview layout. Dimmed, non-selectable and skipped by D-pad; key, saved value and legacy implementation preserved. |
| Remember library views | PreviewPages persists separate Movies/TV view, filters, sort and order. | Active | Existing dynamic preference and default retained. |
| Updates | No updater backend. | Disabled | Coming soon; no misleading action. |
| Streaming enabled / country / provider search / show all / selected providers / preferred provider / catalogue refresh / JustWatch attribution | StreamingPreferences and StreamingRepository retain existing preference objects and saved provider selections. | Disabled | Postponed integration. Existing objects are now retained instead of removeAll; no backend enabled. |
| Integrations | No implemented controls in this category. | Disabled | Coming soon; no fake data/action. |
| Saved network credentials (`share_folders`) | Existing CredentialsManagerPreferenceActivity manages saved share credentials. | Active | Real handler preserved. |
| Advanced mode (`preferences_advanced_video_enabled`) | Existing advanced-mode activation and exit handlers. | Active | Preview no longer clears the saved software-decoding choice when opening Advanced. |
| Licences / acknowledgements | Existing nested licence screen and notices. | Active | Existing navigation and content preserved. |
| Automatic scan on app restart | NetworkAutoRefresh.autoRescanAtStart; Preview retries after offline/busy startup. | Active | Saved default/value unchanged; no automatic enabling. |
| Automatic scan start time / period | NetworkScannerUtil scheduler plus Preview foreground due check. | Active | Existing configured schedule retained; no new arbitrary scan interval. |
| Per-source automatic rescan | ShortcutDbAdapter KEY_RESCAN selects eligible indexed sources. | Active | Existing source selection retained; trigger fix does not widen the indexed-source set. |

## Verification limits

NOT TESTED — USER QA REQUIRED: every preference interaction, saved-value persistence across device-specific capability changes, Trakt/account operations, passthrough/refresh rate/Dolby Vision/3D, subtitle services, SMB/SFTP/VPN combinations, import/export/backup/restore, torrent options and scheduled/offline recovery on the Shield. The audit is grounded in code paths; the minimum smoke checks cover opening Settings and navigating its new presentation.
